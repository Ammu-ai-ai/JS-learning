let text = `This is
a multiline
string`;
console.log(text);
//Backticks are used to define multiline strings without using escape characters.

//single quotes ''
//let msg='javascript's beauty is immaculate';
//WE cant use quotations inside a string
//the interpreter considers the second line as end quote which makes the rest of the lines invalid.
//double quotes
let msg = "Javascript's beauty is immaculate";
//you can also use a forward slash to escape the quote
//the single quotes have more readability than the double quotes
//there is no use of triple quotes
